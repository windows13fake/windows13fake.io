# ☁️ Mon Mini-Cloud Personnel (PHP)

Ceci est une implémentation minimale d'un système de stockage de fichiers (cloud) utilisant PHP. 

Il permet :
* Une authentification par mot de passe simple.
* Le téléversement de fichiers sur le serveur.
* L'affichage et le téléchargement des fichiers stockés.

## ⚠️ AVERTISSEMENT IMPORTANT

**Ce système n'est pas sécurisé pour une utilisation en production ou pour des données sensibles !** Le mot de passe est en clair dans le code PHP. C'est un projet d'apprentissage.

## 🛠️ Configuration Requise

Pour que ce projet fonctionne, il doit être hébergé sur un **serveur web** (comme Apache ou Nginx) avec **PHP installé**. Il ne fonctionnera **PAS** en utilisant GitHub Pages seul.

### Étapes d'Installation
1.  Téléversez tous les fichiers et dossiers sur votre serveur.
2.  Assurez-vous que le dossier **`fichiers/`** a les permissions d'écriture (souvent `chmod 777` ou `775`) par le serveur.

## 🔑 Accès

* **Code d'accès :** `160902`

## 💖 Personnalisation

Le fond d'écran après connexion utilise un fichier image nommé `maman_je_taime.jpg` dans le dossier racine.
